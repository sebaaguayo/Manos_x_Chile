Beveled boxed form field — 14px corners, border turns ocean-blue with a soft halo on focus.

```jsx
<Input placeholder="Nombre" />
<Input as="textarea" rows={4} placeholder="Mensaje" />
<Input underline placeholder="Minimal variant" />
```

Default is boxed. `underline` for bottom-rule; `as="textarea"` for multiline.
