Single impact statistic — big bold vivid number over an uppercase tracked label.

```jsx
<Stat value="500+" label="Ton rescatadas / año" />
<Stat value="25+" label="Años de vida útil" color="blue" size="lg" align="center" />
```

Color `green` | `blue` | `dark`. Sizes `sm`/`md`/`lg`.
