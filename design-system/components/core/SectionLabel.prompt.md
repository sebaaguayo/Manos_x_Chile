Bold uppercase tracked eyebrow above section headings (vivid green by default).

```jsx
<SectionLabel>Materias Primas</SectionLabel>
<SectionLabel color="blue">Productos</SectionLabel>
```

Color: `green` | `blue` | `muted` | `dark`. Pairs with bold all-caps headings.
