Bold beveled action button — 14px corners, heavy Black uppercase tracked label, ocean-blue fill.

```jsx
<Button>Cotizar</Button>                {/* blue */}
<Button variant="eco">Ver Catálogo</Button>   {/* green */}
<Button variant="outline">Saber más</Button>
<Button variant="outline-light">Sobre imagen</Button>
<Button size="lg" fullWidth>Enviar</Button>
```

Variants: `primary` (blue), `eco` (green), `outline`, `outline-green`, `outline-light` (for dark heroes), `ghost`. Sizes `sm`/`md`/`lg`. `as="a"`, `fullWidth`, `uppercase={false}`.
